import type { AssessmentStatus } from "@/lib/domain/status";
export function nextActionFor(status:AssessmentStatus){const copy={FIELD_SURVEY_REQUIRED:["Schedule field survey","The site identity was resolved and useful digital context was collected. Physical engineering and site facts still require a field survey."],ADDRESS_CORRECTION_REQUIRED:["Correct installation address","The submitted address could not be reliably resolved, so site-specific enrichment was safely stopped."],MANUAL_DATA_REVIEW:["Send to manual data review","The site was resolved, but important digital enrichment was unavailable or conflicted."],OUTSIDE_SERVICE_AREA:["Route to out-of-area sales process","The resolved site is outside the configured InstallIQ operating area."],SYSTEM_ERROR:["Retry or contact system support","The assessment encountered an unrecoverable application error."]} as const;return {nextAction:copy[status][0],explanation:copy[status][1]};}
