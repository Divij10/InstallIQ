export function haversineMiles(aLat: number, aLng: number, bLat: number, bLng: number) { const r = 3958.7613; const rad = (n: number) => n * Math.PI / 180; const dLat = rad(bLat - aLat), dLng = rad(bLng - aLng); const x = Math.sin(dLat / 2) ** 2 + Math.cos(rad(aLat)) * Math.cos(rad(bLat)) * Math.sin(dLng / 2) ** 2; return 2 * r * Math.asin(Math.sqrt(x)); }
