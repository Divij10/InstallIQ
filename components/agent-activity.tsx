import type { AgentTraceEvent } from "@/lib/domain/trace";
export function AgentActivity({events}:{events:AgentTraceEvent[]}){return <section className="panel activity"><div className="card-heading"><h2>Action log</h2><span>{events.length} events</span></div><ol>{events.map(event=><li key={event.id}><b className={event.type}/><div><strong>{event.message}</strong><small>{event.skillName??event.phase} · {new Date(event.timestamp).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit",second:"2-digit"})}</small></div></li>)}</ol></section>}
