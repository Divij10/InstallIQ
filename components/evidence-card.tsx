import type { Evidence } from "@/lib/domain/evidence";
export function EvidenceCard({evidence}:{evidence:Evidence}){return <details className="evidence"><summary><span>Source · {evidence.source.provider} / {evidence.source.capability}</span><em className={evidence.status}>{evidence.status.replaceAll("_"," ")}</em></summary><p>{evidence.message??"Normalized evidence retained for inspection."}</p><small>{evidence.source.actionName&&`Precisely action: ${evidence.source.actionName} · `}{evidence.source.toolName&&`MCP tool: ${evidence.source.toolName} · `}{new Date(evidence.retrievedAt).toLocaleString()}</small>{evidence.rawAvailable&&<pre>{JSON.stringify(evidence.raw??evidence.data,null,2)}</pre>}</details>}
